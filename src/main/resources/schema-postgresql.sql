CREATE TABLE IF NOT EXISTS image_metadata (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    filename VARCHAR(255) NOT NULL,
    github_url VARCHAR(1000) NOT NULL,
    github_path VARCHAR(1000) NOT NULL,
    github_sha VARCHAR(255),
    upload_date TIMESTAMP
);

ALTER TABLE image_metadata ADD COLUMN IF NOT EXISTS filename VARCHAR(255);
ALTER TABLE image_metadata ADD COLUMN IF NOT EXISTS github_url VARCHAR(1000);
ALTER TABLE image_metadata ADD COLUMN IF NOT EXISTS github_path VARCHAR(1000);
ALTER TABLE image_metadata ADD COLUMN IF NOT EXISTS github_sha VARCHAR(255);
ALTER TABLE image_metadata ADD COLUMN IF NOT EXISTS upload_date TIMESTAMP;

CREATE TABLE IF NOT EXISTS daily_analytics (
    date_id DATE PRIMARY KEY,
    total_views BIGINT NOT NULL DEFAULT 0,
    unique_visitors BIGINT NOT NULL DEFAULT 0,
    user_logins BIGINT NOT NULL DEFAULT 0
);

ALTER TABLE daily_analytics ADD COLUMN IF NOT EXISTS total_views BIGINT NOT NULL DEFAULT 0;
ALTER TABLE daily_analytics ADD COLUMN IF NOT EXISTS unique_visitors BIGINT NOT NULL DEFAULT 0;
ALTER TABLE daily_analytics ADD COLUMN IF NOT EXISTS user_logins BIGINT NOT NULL DEFAULT 0;

CREATE TABLE IF NOT EXISTS visitor_sessions (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    page VARCHAR(255),
    browser VARCHAR(255),
    os VARCHAR(255),
    device VARCHAR(255),
    language VARCHAR(255),
    referrer VARCHAR(255),
    ip_address VARCHAR(255),
    user_email VARCHAR(255),
    system_info TEXT,
    "timestamp" TIMESTAMP
);

ALTER TABLE visitor_sessions ADD COLUMN IF NOT EXISTS page VARCHAR(255);
ALTER TABLE visitor_sessions ADD COLUMN IF NOT EXISTS browser VARCHAR(255);
ALTER TABLE visitor_sessions ADD COLUMN IF NOT EXISTS os VARCHAR(255);
ALTER TABLE visitor_sessions ADD COLUMN IF NOT EXISTS device VARCHAR(255);
ALTER TABLE visitor_sessions ADD COLUMN IF NOT EXISTS language VARCHAR(255);
ALTER TABLE visitor_sessions ADD COLUMN IF NOT EXISTS referrer VARCHAR(255);
ALTER TABLE visitor_sessions ADD COLUMN IF NOT EXISTS ip_address VARCHAR(255);
ALTER TABLE visitor_sessions ADD COLUMN IF NOT EXISTS user_email VARCHAR(255);
ALTER TABLE visitor_sessions ADD COLUMN IF NOT EXISTS system_info TEXT;
ALTER TABLE visitor_sessions ADD COLUMN IF NOT EXISTS "timestamp" TIMESTAMP;

CREATE TABLE IF NOT EXISTS prompts (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    media_url VARCHAR(255) NOT NULL,
    media_type VARCHAR(255) NOT NULL,
    ai_model VARCHAR(255),
    prompt_text TEXT NOT NULL,
    category VARCHAR(255),
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

ALTER TABLE prompts ADD COLUMN IF NOT EXISTS title VARCHAR(255);
ALTER TABLE prompts ADD COLUMN IF NOT EXISTS description TEXT;
ALTER TABLE prompts ADD COLUMN IF NOT EXISTS media_url VARCHAR(255);
ALTER TABLE prompts ADD COLUMN IF NOT EXISTS media_type VARCHAR(255);
ALTER TABLE prompts ADD COLUMN IF NOT EXISTS ai_model VARCHAR(255);
ALTER TABLE prompts ADD COLUMN IF NOT EXISTS prompt_text TEXT;
ALTER TABLE prompts ADD COLUMN IF NOT EXISTS category VARCHAR(255);
ALTER TABLE prompts ADD COLUMN IF NOT EXISTS created_at TIMESTAMP;
ALTER TABLE prompts ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP;
